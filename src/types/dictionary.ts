export interface Dictionary {
  nav: {
    blog: string;
    about: string;
    contact: string;
  };
  home: {
    title: string;
    description: string;
  };
  about: {
    title: string;
    description: string;
  };
  contact: {
    title: string;
    description: string;
  };
} 